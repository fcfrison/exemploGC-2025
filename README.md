### Para rodar isso:
$ ./ run.x {arquivo.cmm}
$ ./ {arquivo executável}

### formas de implementar x += y
    a = a + (exp)
    a = (exp) + a     >>>     essa é mais fácil, não perca tempo com a outra
